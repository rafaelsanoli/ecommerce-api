spring:
  application:
    name: ecommerce-api

  datasource:
    url: jdbc:h2:mem:ecommerce
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
      data-locations: classpath:data.sql

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha

logging:
  level:
    com.ecommerce.api: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG

# JWT Configuration
jwt:
  secret: ecommerce-api-secret-key-change-in-production-minimum-256-bits-required-for-hs256-algorithm
  expiration: 86400000  # 24 horas em milissegundos
